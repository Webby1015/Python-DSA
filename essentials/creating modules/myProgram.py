import functions

# if functions was no in the same directiory
# we would write foldername.filename.py
# if the file is somewhere random on system we would write its entire path
# import sys
# sys.path.append("c:\Code")
# now we can import the functions like normal (import functions)
print(functions.square_area(4))
print(functions.triangle_area(4, 5))
